spring:
  application:
    name: order-service
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848 #Nacos配置中心地址
        file-extension: yaml #文件扩展格式
    alibaba:
      seata:
        tx-service-group: testAAA

  profiles:
    active: dev